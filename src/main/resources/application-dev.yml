spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/claimhub}
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.vega.claimhub: DEBUG
